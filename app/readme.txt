build文件夹:编译后的文件存在的位置。

libs文件夹:依赖的库所在的位置（jar和aar)。

release文件夹:发行版本的最终APK文件所在位置

src文件夹:源代码所在的目录。

src/main文件夹:主要代码所在位置。

src/main/java文件夹:编写的java源代码所在位置。

src/main/jniLibs文件夹:jni的一些动态库所在的默认位置。

src/main/res文件夹:android资源文件所在位置。

build.gradle文件:和这个项目有关的gradle配置。

proguard.pro文件:代码混淆配置文件